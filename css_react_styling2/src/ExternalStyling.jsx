import React from 'react';
import '../src/styles/ExternalStyling.css';
function ExternalStyling(props) {
    return (
        <div>
            <h2>Now let's move on to external styling in React JS</h2>
            <h2>In external styling, the styles are defined in a separate file with <b>.css</b> extension. The file can be imported and style can then be applied.</h2>
        </div>
    );
}

export default ExternalStyling;