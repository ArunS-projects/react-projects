import React from 'react';

function JSXfragment(props) {
    return (
        <>
        The empty tags are also called as JSX Fragment. These empty tags are identified as an alternative to 'div' tags in .jsx
        JSX follows XML rules and therefore all the HTML tags must be properly closed. Otherwise JSX will throw errors.
        </>
    );
}

export default JSXfragment;